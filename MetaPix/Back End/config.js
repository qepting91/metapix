module.exports = {
    API_KEY: "YOUR_API_KEY",
    API_SECRET: "YOUR_API_SECRET",
    PORT: process.env.PORT || 3000,
  };

  /*
  This file exports an object containing the API key, API secret, and the port number for the backend. 
  The port number is set to use the environment variable PORT if it exists, or default to 3000. 
  Replace YOUR_API_KEY and YOUR_API_SECRET with the actual API key and secret you have obtained 
  from the reverse image search API you are using.
  */
